<h1>Wikinimous</h1>

<%= link_to 'New article', new_article_path %>

<% @articles.each do |article| %>
  <div>
    <div><h4><%= link_to article.title, article_path(article) %></h4></div>
  </div>
<% end %>
